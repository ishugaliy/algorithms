package dp;

import java.lang.reflect.Array;
import java.util.*;

// TODO:
public class DividingIntoFairTeams {

    public static void main(String[] args) {
        List<Integer> list = Arrays.asList(2, 3, 1);
        list.sort(Comparator.naturalOrder());
        new LinkedHashSet<>(list);
    }
}
